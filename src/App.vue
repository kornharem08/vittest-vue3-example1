<script setup lang="ts">
import BaseModal from './components/BaseModal.vue';
import { useModal } from './components/useModal';

const { modalRef: modalRef1, showModal: showModal1 } = useModal();
const { modalRef: modalRef2, showModal: showModal2 } = useModal();

const openModal = (id: string) => {
  if (id === 'modal1') {
    showModal1();
  } else if (id === 'modal2') {
    showModal2();
  }
};


</script>

<template>
   <div class="p-4">
    <button @click="openModal('modal1')" class="px-4 py-2 bg-blue-500 text-white rounded">
      Open Modal 1
    </button>

    <BaseModal ref="modalRef1">
      <h2 class="text-xl font-bold">Modal 1</h2>
      <p>This is the content of Modal 1.</p>
      <button @click="openModal('modal2')" class="ml-2 px-4 py-2 bg-green-500 text-white rounded">
      Open Modal 2
    </button>

    </BaseModal>

    <BaseModal ref="modalRef2">
      <h2 class="text-xl font-bold">Modal 2</h2>
      <p>This is the content of Modal 2.</p>
    </BaseModal>
  </div>
    <form class="max-w-sm mx-auto">
  <label for="countries" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Select an option</label>
  <select id="countries" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500">
    <option selected>Choose a country</option>
    <option value="US">United States</option>
    <option value="CA">Canada</option>
    <option value="FR">France</option>
    <option value="DE">Germany</option>
  </select>
</form>

  <BaseModal modalId="test1"/>
</template>
